# Caddy host ports.
# Port 80 is needed for normal HTTP access to *.ieee.local names.
# Port 443 is reserved for future HTTPS/internal CA work if we add it later.
CADDY_HTTP_PORT=80
CADDY_HTTPS_PORT=443
